{
  "name": "purgedcv-backtest-dashboard",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Verdict-first dashboard for purgedcv-backtest reports.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "export": "tsc --noEmit && vite build && node scripts/standalone.mjs"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "vite": "^7.1.0"
  }
}
